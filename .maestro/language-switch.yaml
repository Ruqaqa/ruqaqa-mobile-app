appId: sa.ruqaqa.finance
---
# Test language switching between English and Arabic
- launchApp
- extendedWaitUntil:
    visible: "Sign in with SSO|Update now"
    timeout: 10000
- tapOn:
    id: "dev-skip-update"
    optional: true

# Verify English content
- assertVisible: "Sign in with Microsoft"

# Switch to other language
- tapOn:
    id: "language-switcher"

# Wait for language change
- extendedWaitUntil:
    visible: "تسجيل الدخول بـ Microsoft"
    timeout: 5000

# Switch back
- tapOn:
    id: "language-switcher"
- extendedWaitUntil:
    visible: "Sign in with Microsoft"
    timeout: 5000
